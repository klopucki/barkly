apiVersion: v1
kind: Service

metadata:
  name: barkly-api

spec:
  selector:
    app: barkly-api

  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.service.port }}

  type: ClusterIP