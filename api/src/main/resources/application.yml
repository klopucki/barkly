spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/barkly
    username: barkly
    password: barkly

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration